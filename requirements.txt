# ============================================
# 📦 BITE STORE BOT — Required Packages
# ============================================
# 🆕 v43: PTB >= 22.7 needed for icon_custom_emoji_id on InlineKeyboardButton
# (Bot API 9.4, Feb 2026 — premium emoji icons in buttons).
python-telegram-bot[job-queue]>=22.7
google-generativeai
python-dotenv
Pillow
requests
# 🆕 v61: enable SOCKS5 proxies for Binance API (Pakistani proxy support)
requests[socks]
PySocks
# 🆕 v70: QR code generation for product share links
qrcode


# 🆕 v75: aiohttp removed — bot runs as Render Background Worker (no HTTP server).
